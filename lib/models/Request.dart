class Request {
  String name, message, country, phone, formattedDate, job, latitude, longitude;
  Request(
    this.name,
    this.job,
    this.message,
    this.country,
    this.phone,
    this.formattedDate,
    this.latitude,
    this.longitude,
  );
}
